package com.rain.sdk.sample.ui.theme

import androidx.compose.ui.unit.dp

/** Corner radii: three tiers only (the third is the full pill). */
object RainRadius {
    val Small = 4.dp // inputs, badges, tiles
    val Card = 20.dp // cards, panels, dialogs
}
