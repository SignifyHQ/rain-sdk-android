# R8 rules for the sample app's minified release build (the SDK's consumer check in CI).
# Keep this file minimal: rules an R8-shrunk host app genuinely needs should live in the
# SDK modules' consumer-rules.pro so every consumer inherits them — not here.
